# CLIENT

`cd client`

`npm install`

`npm run dev`

## Server

/server/.env

```
PORT = 8080

TYPE = postgres
USERNAME = postgres
HOST = localhost
PASSWORD = ****
DB_PORT = 3030
DATABASE = movieDb

```

`cd server`

`npm install`

`npm run dev`

http://localhost:8080/movie/load

Initially :- Run this api to load all data in own database
